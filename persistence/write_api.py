"""
Explicit WRITE APIs (Phase-I)

NOTE:
Player creation & updates are handled via read_api.py
This file is intentionally minimal to avoid duplication.
"""

from fastapi import APIRouter

router = APIRouter()
